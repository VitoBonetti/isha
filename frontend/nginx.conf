server {
    # Cloud Run forwards traffic to port 8080 by default
    listen 8080;
    server_name _;

    # Where the React build files live
    root /usr/share/nginx/html;
    index index.html;

    # React Router support: if a file isn't found, send them to index.html
    location / {
        try_files $uri /index.html;
    }

    # Optional but recommended: Cache static assets (images, css, js) for performance
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, no-transform";
    }
}